import React, { Component } from 'react';
import './styles.less';

class Playlist extends Component {
  render() {
    return(
      <section id='playlist'>
        playlist
      </section>
    )
  }
}

export default Playlist;
